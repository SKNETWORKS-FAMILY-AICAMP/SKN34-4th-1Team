<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.govbiz.core.account.repository.mapper.AccountPasswordResetMapper">

    <resultMap id="accountPasswordResetDbRow" type="ai.govbiz.core.account.repository.mapper.AccountPasswordResetDbRow">
        <id property="id" column="id" />
        <result property="accountId" column="account_id" />
        <result property="tokenHash" column="token_hash" />
        <result property="expiresAt" column="expires_at" />
        <result property="usedAt" column="used_at" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <insert id="insertReset"
            parameterType="ai.govbiz.core.account.repository.mapper.AccountPasswordResetDbRow"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO account_password_reset (
            account_id,
            token_hash,
            expires_at,
            created_at
        ) VALUES (
            #{accountId},
            #{tokenHash},
            #{expiresAt},
            #{createdAt}
        )
    </insert>

    <select id="countResetsSince" resultType="int">
        SELECT COUNT(*)
        FROM account_password_reset
        WHERE account_id = #{accountId}
          AND created_at &gt;= #{since}
    </select>

    <select id="findActiveResetByTokenHash" resultMap="accountPasswordResetDbRow">
        SELECT id, account_id, token_hash, expires_at, used_at, created_at
        FROM account_password_reset
        WHERE token_hash = #{tokenHash}
          AND used_at IS NULL
          AND expires_at &gt; #{now}
        FOR UPDATE
    </select>

    <delete id="deleteResetsByAccountId">
        DELETE FROM account_password_reset
        WHERE account_id = #{accountId}
    </delete>
</mapper>
